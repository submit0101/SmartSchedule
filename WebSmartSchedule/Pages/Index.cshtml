@page
@model IndexModel
@{
    ViewData["Title"] = "Главная";
}

@if (User.Identity?.IsAuthenticated == true)
{
    @* Вставляем деталь дашборда *@
    <partial name="_Dashboard" />
}
else
{
    @* Вставляем деталь лендинга *@
    <partial name="_GuestLanding" />
}