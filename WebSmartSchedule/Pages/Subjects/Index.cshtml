@page
@model WebSmartSchedule.Pages.Subjects.IndexModel
@{
    ViewData["Title"] = "Предметы";
}
<link rel="stylesheet" href="~/css/UnionStyle.css" />
<div class="container-xxl py-4">
    <div class="page-section p-4 bg-white rounded shadow-sm">

        <!-- Сообщения -->
        @if (TempData["SUSuccessMessage"] != null)
        {
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                @TempData["SUSuccessMessage"]
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        }
        @if (TempData["SUErrorMessage"] != null)
        {
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                @TempData["SUErrorMessage"]
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        }

        <!-- Заголовок и кнопка добавления -->
        <div class="d-flex flex-wrap justify-content-between align-items-center mb-4 gap-2">
            <h2 class="mb-0 text-primary fw-bold">📚 Список предметов</h2>
            <a href="#"
               class="btn btn-success px-3 py-2 d-flex align-items-center gap-2"
               onclick="openModal('createSubjectModal')">
                <i class="bi bi-plus-lg"></i> Добавить предмет
            </a>
        </div>
        <div class="row mb-3">
            <div class="col-md-6 col-lg-4">
                <div class="input-group">
                    <span class="input-group-text bg-white border-end-0">

                    </span>
                    <input type="text"
                           id="searchInput"
                           class="form-control border-start-0 ps-0"
                           placeholder="Найти...">
                    <button class="btn btn-outline-primary"
                            type="button"
                            id="searchButton"
                            onclick="performSearch()">
                        <i class="bi bi-search"></i>
                    </button>
                </div>
            </div>
        </div>

        <!-- Отображение результатов -->
        @if (!Model.Subjects.Any())
        {
            <div class="text-center py-5 bg-light rounded">
                <p class="lead mb-2">Нет данных о предметах.</p>
                <p class="text-muted">Добавьте их через Swagger или миграции.</p>
            </div>
        }
        else
        {
            <!-- Карточки для мобильных устройств -->
            <div class="d-block d-md-none mt-3" style="max-height: 60vh; overflow-y: auto;">
                <div class="container-fluid p-0">
                    @foreach (var subject in Model.Subjects)
                    {
                        <div class="card mb-3 shadow-sm">
                            <div class="card-body">
                                <h5 class="card-title">@subject.Title</h5>
                                <div class="d-flex gap-2 mt-3">
                                    <button class="btn btn-sm btn-outline-primary d-flex align-items-center gap-1"
                                            onclick="fillEditModal(this)"
                                            data-id="@subject.Id"
                                            data-title="@subject.Title">
                                        <i class="bi bi-pencil"></i> Изменить
                                    </button>
                                    <button type="button" class="btn btn-outline-danger btn-sm"
                                            onclick="confirmDelete(@subject.Id)">
                                        Удалить
                                    </button>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>

            <!-- Таблица для десктопа -->
            <div class="table-container border rounded overflow-auto d-none d-md-block mt-3" style="max-height: 60vh;">
                <table class="table table-hover align-middle mb-0">
                    <thead class="table-light sticky-top">
                        <tr>
                            <th>Название</th>
                            <th>Действия</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var subject in Model.Subjects)
                        {
                            <tr>
                                <td data-label="Название">@subject.Title</td>
                                <td data-label="Действия">
                                    <div class="d-flex justify-content-center gap-2">
                                        <button class="btn btn-sm btn-outline-primary"
                                                onclick="fillEditModal(this)"
                                                data-id="@subject.Id"
                                                data-title="@subject.Title">
                                            <i class="bi bi-pencil"></i> Изменить
                                        </button>
                                        <button type="button" class="btn btn-outline-danger btn-sm"
                                                onclick="confirmDelete(@subject.Id)">
                                            Удалить
                                        </button>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
    </div>
</div>

<partial name="_SubjectModals" />

<script>
    function fillEditModal(button) {
        const id = button.getAttribute('data-id');
        const title = button.getAttribute('data-title');

        document.getElementById('editSubjectId').value = id;
        document.getElementById('editSubjectTitle').value = title;

        openModal('editSubjectModal');
    }

    function confirmDelete(id) {
        document.getElementById('deleteSubjectId').value = id;
        openModal('deleteConfirmModal');
    }
</script>

<script src="/js/modal.js"></script>
<script src="/js/searchHandler.js"></script>
<script src="/js/ValidationModal.js"></script>
